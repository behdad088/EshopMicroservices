apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-query-eventprocessor
  namespace: eshop-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-query-eventprocessor
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: order-query-eventprocessor
    spec:
      serviceAccountName: sa-order-query-eventprocessor
      initContainers:
        - name: wait-for-db
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z order-query-db 5432; do echo waiting for order-query-db; sleep 2; done']
        - name: wait-for-rabbitmq
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z rabbitmq 5672; do echo waiting for rabbitmq; sleep 2; done']
      containers:
        - name: order-query-eventprocessor
          image: order.query.eventprocessor:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: order-query-eventprocessor-cm
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: order-query-db-secret
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-query-db-secret
                  key: POSTGRES_PASSWORD
            - name: ConnectionStrings__Database
              value: "Host=order-query-db;Port=5432;Database=OrderQueryDb;User Id=$(DB_USER);Password=$(DB_PASSWORD)"
            - name: RabbitMQ__Username
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: RABBITMQ_USERNAME
            - name: RabbitMQ__Password
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: RABBITMQ_PASSWORD
            - name: ASPNETCORE_Kestrel__Certificates__Default__Password
              valueFrom:
                secretKeyRef:
                  name: cert-password
                  key: CERT_PASSWORD
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 8081
              name: https
          livenessProbe:
            httpGet:
              path: /hc
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /hc
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 300m
              memory: 512Mi
          volumeMounts:
            - name: certs
              mountPath: /app/certificates
              readOnly: true
      volumes:
        - name: certs
          projected:
            sources:
              - secret:
                  name: cert-order-query-eventprocessor
              - configMap:
                  name: cert-ca
