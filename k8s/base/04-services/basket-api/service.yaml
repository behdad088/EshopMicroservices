apiVersion: v1
kind: Service
metadata:
  name: basket-api
  namespace: eshop-microservices
spec:
  selector:
    app: basket-api
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 8081
      targetPort: 8081
      name: https
