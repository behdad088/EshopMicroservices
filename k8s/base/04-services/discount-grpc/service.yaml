apiVersion: v1
kind: Service
metadata:
  name: discount-grpc
  namespace: eshop-microservices
spec:
  selector:
    app: discount-grpc
  ports:
    - port: 8080
      targetPort: 8080
      name: grpc
      appProtocol: kubernetes.io/h2c
    - port: 8081
      targetPort: 8081
      name: https
